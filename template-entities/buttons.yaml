- button:
    - name: Vacuum Reset Dock Cleanliness
      unique_id: button.vacuum_reset_dock_cleanliness
      press:
        - condition: template # to avoid template reloads resulting in button presses
          value_template: "{{ this.state != 'unavailable' }}"
        - action: rest_command.vacuum_dock_cleaning